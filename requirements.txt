# 抖音图文创作顾问 Agent 依赖
# 安装：pip install -r requirements.txt

# ============================================================
# 核心依赖（必需）
# ============================================================

# 浏览器自动化
playwright>=1.40.0  # 用于自动化操作抖音网页端

# HTTP 请求
requests>=2.31.0  # DeepSeek API 调用

# Web 服务（后端 API）
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6
websockets>=12.0

# 环境变量
python-dotenv>=1.0.0

# ============================================================
# 可选依赖（降级运行，不影响核心功能）
# ============================================================

# Redis 缓存（需 Redis 服务运行在 6379 端口）
redis>=5.0.0

# Qdrant 向量数据库（语义检索，需 Qdrant 服务运行在 6333 端口）
qdrant-client>=1.7.0

# 文本向量化（Qdrant 依赖，约 100MB 模型下载）
sentence-transformers>=2.7.0

# 图片处理（可选）
# Pillow>=10.0.0