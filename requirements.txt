# 抖音图文创作顾问 Agent 依赖

# 浏览器自动化
playwright>=1.40.0  # 用于自动化操作抖音网页端

# HTTP 请求
requests>=2.31.0  # DeepSeek API 调用

# Web 服务（后端 API）
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6
websockets>=12.0

# SQLite（Python 标准库，无需额外安装）

# 可选：
# Pillow>=10.0.0  # 图片处理
# openai>=1.0.0  # AI 接口（如需要）